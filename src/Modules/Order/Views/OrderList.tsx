import { useAppDispatch, useAppSelector } from "Services/Hook/Hook";
import { getOrderListAction } from "../Reducer/OrderAction";

type Props = {}

const OrderList = (props: Props) => {
    return (
        <div>OrderList</div>
    )
}

export default OrderList